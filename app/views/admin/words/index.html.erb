
<br><br>
<div class="col-lg-12 mx-auto">
    <div class="card card-signin col-lg-12 mx-auto">
        <br><br>
        <div class = "row">
            <div class = "col">
            <h3 class="card-title text-left">Category | <%= @category.title %></h3>
                   
            </div>

            <div class = "col">
                <%= link_to "Back", admin_categories_path, class: "btn btn-outline-info btn-md" %>
                <%= link_to "Add word", new_admin_category_word_path(@category), class: "btn btn-secondary btn-md" %>
            </div>
        </div>

        <table class="table table-striped">
            <thead>
                <tr>
                    <th scope="col">Words</th>
                    <th scope="col">Answer</th>
                    <th scope="col">Action</th>
                </tr>
            </thead>
            <tbody>
            <%if @words.any? %>
            <%= render @words %>
            <% else %>
                <tr>
                   <td></td>
                   <td>No words in this category yet</td> 
                   <td></td>
                </tr>
            <% end %>
            </tbody>
        </table>
        <%= will_paginate @words%>
    <div class="card card-signin flex-row my-5">
</div>