<tr id="word-<%= word.id %>">
    <td><%= word.word %></td>
    <td><%= word.answer_right || "No answer" %></td>
    <td><%= link_to "Edit", edit_admin_category_word_path(@category, word), class: "btn btn-warning btn-sm"%> <%= link_to "Delete", [:admin, @category, word], method: :delete,
    data: { confirm: "You sure?" } %> </td>
</tr>